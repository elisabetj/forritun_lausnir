n = int(input())  # Do not change this line
print("Primes in the range", n ** 2, "and", (n + 1) ** 2, "are:")

# Initialize a counter

# Repeat until the counter reaches some limit
    is_prime = True
    # Fill in the missing code below

    if is_prime:
        # Output something

    # Don't forget to adjust the counter
